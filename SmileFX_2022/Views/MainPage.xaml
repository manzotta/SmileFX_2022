<Page
    x:Class="SmileFX_2022.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SmileFX_2022.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:chart="using:Syncfusion.UI.Xaml.Charts"
    mc:Ignorable="d"
    xmlns:vm="using:SmileFX_2022.ViewModels" 
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.DataContext>
        <vm:MainPageViewModel x:Name="MainVM"/>
    </Page.DataContext>
    
    <Grid x:Name="ParentGrid">


        <NavigationView x:Name="nvSample" Loaded="nvSample_Loaded" 
                        SelectionChanged="nvSample_SelectionChanged" PaneDisplayMode="Left"
                        IsSettingsVisible="False">
            <NavigationView.MenuItems>
                <NavigationViewItem  Content="Instruments" Tag="page1"/>
                <NavigationViewItem  Content="Trades" Tag="page2" />
            </NavigationView.MenuItems>
            <ScrollViewer>
                <Frame x:Name="ContentFrame"/>
            </ScrollViewer>
        </NavigationView>

    </Grid>

</Page>
